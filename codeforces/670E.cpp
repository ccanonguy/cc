#include <stdc++.h>
using namespace std;

int main() {
	stack<char> seq;
	string bracket;
	cin >> bracket;
	int pos[bracket.length()];
	for (int i=0; i<bracket.length(); i++) {
		if (bracket[i] == '(')
	}
}
