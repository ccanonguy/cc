#include <bits/stdc++.h>
using namespace std;

class Circuits {
	private:
	
	
	public:
	int howLong(String[] connects, String[] costs) {
		int elemConnect = 0;
		int result = -999;
		while (!connects[elemConnect].empty()) elemConnect++;
		for (int i=0; i<elemConnects; i++) {
			if (search())
		}
	}
}

int main() {
	
}
